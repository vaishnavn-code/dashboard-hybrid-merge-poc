# Vite exposes only variables prefixed with VITE_
# The backend URL is proxied via vite.config.js in dev.
# For production builds, set this to your deployed API base URL.
VITE_API_BASE_URL=http://localhost:8000
